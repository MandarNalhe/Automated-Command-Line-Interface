package com.mcp.host.mcp_host.Services;

import com.mcp.host.mcp_host.model.ExecutionPlan;
import com.mcp.host.mcp_host.tools.CMD_EXEC;


public class Input {
    public static void main(String[] args) throws Exception {
       // GenerateTextFromTextInput.inputToJSON("");
     /*   Orchestrator orchestrator = new Orchestrator();
        ExecutionPlan plan = orchestrator.getExecutionPlan("Network Scan google.com and explain the findings ");
        CMD_EXEC exec = new CMD_EXEC(System.getProperty("user.home"));
        exec.executePlan(plan);*/
    }
}
